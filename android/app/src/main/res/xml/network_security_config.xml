<?xml version="1.0" encoding="utf-8"?>
<!--
  DROP network security config.

  Production builds only talk HTTPS (Supabase + signed storage URLs), which
  is allowed by default. The debug override permits the local dev server
  (http://10.0.2.2 / localhost) so `cap run android` works against a local
  backend without weakening release builds.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
